.openbar/
target/
node_modules/
dist/
.DS_Store
# Tauri build-generated schemas/config (regenerated by tauri-build).
app/src-tauri/gen/
# Sidecar binaries staged by app/scripts/build-sidecar.mjs on every dev/build.
app/src-tauri/binaries/
# Vite's dependency-optimizer cache (regenerated on every dev run).
.vite/
# Agent worktrees: full checkouts, gigabytes, never part of the source.
.claude/worktrees/
