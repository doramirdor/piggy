# Project rules

The long paragraph here is the same block as the global half of the pair, with
different line breaks and some extra spaces inside it.

Never reproduce a number the model  produced; every figure shown to a person
must trace back to   the database, the probe, or the scanner that measured it.

Run the tests before you commit.

This paragraph belongs to the project file alone, and like its opposite number
it is long enough to be hashed and still must not be called a duplicate.
